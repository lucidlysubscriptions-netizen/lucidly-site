// Feature flags for archived UI.
//
// SHOW_SECTION_META - the "/ NN" + label meta column shown to the left of
// each section header (and inside Frame, Execution Engine, and Backed).
// Archived 2026-05-20 at the user's request. Flip back to `true` to restore.
export const SHOW_SECTION_META = false
